# <!-- @GENESIS_MODULE_START: ultra_minimal_test -->

from datetime import datetime\n#!/usr/bin/env python3
"""
Ultra-minimal test for MetaSignalHarmonizer
"""
from event_bus import EventBus

import sys
import os

print("🔧 Starting ultra-minimal test...")

try:
    # Add current directory to path
    sys.path.insert(0, os.getcwd())
    print("✅ Path setup complete")
    
    # Test core directory
    if os.path.exists("core"):
        print("✅ Core directory found")
    else:
        print("❌ Core directory not found")
        print("Creating core directory...")
        os.makedirs("core", exist_ok=True)
    
    # Test meta signal harmonizer file
    harmonizer_file = "core/meta_signal_harmonizer.py"
    if os.path.exists(harmonizer_file):
        print("✅ MetaSignalHarmonizer file found")
        
        # Try importing
        try:
            from core.meta_signal_harmonizer import MetaSignalHarmonizer
            print("✅ MetaSignalHarmonizer class imported successfully")
            
            # Try creating instance
            harmonizer = MetaSignalHarmonizer()
            print("✅ MetaSignalHarmonizer instance created successfully")
            
            print("\n🎉 ULTRA-MINIMAL TEST PASSED")
            print("✅ MetaSignalHarmonizer module is functional")
            
        except ImportError as e:
            print(f"❌ Import error: {e}")
        except Exception as e:
            print(f"❌ Instance creation error: {e}")
    else:
        print(f"❌ MetaSignalHarmonizer file not found at: {harmonizer_file}")
        
except Exception as e:
    print(f"💥 Critical error: {e}")

print("🔧 Test completed.")

    def log_state(self):
        """Phase 91 Telemetry Enforcer - Log current module state"""
        state_data = {
            "module": __name__,
            "timestamp": datetime.now().isoformat(),
            "status": "active",
            "phase": "91_telemetry_enforcement"
        }
        if hasattr(self, 'event_bus') and self.event_bus:
            self.event_bus.emit("telemetry", state_data)
        return state_data
        

# <!-- @GENESIS_MODULE_END: ultra_minimal_test -->